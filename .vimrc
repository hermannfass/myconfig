" Set encoding to UTF-8:
set enc=utf-8
" Go to directory of choice (or not):
" cd ~/_local_working_copy/MeineDokumente
" Convert tabs into spaces (use <C-v><Tab> to insert a real tab:
set expandtab
" Show line and column at the status line:
set ruler
" text width, i.e. where words will be wrapped:
set tw=78
" tab stop, i.e. how many spaces the <Tab> key enters:
set ts=4
" shift width, i.e. how many spaces are inserted when indenting:
set sw=4

